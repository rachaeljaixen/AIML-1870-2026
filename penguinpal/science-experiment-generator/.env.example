# Science Experiment Generator — Environment Configuration
# Copy this file, name it .env, and fill in your API key.
# Load it in the app using the "Load .env" button.
# The key is stored in memory only and cleared on page refresh.

OPENAI_API_KEY=sk-your-api-key-here
